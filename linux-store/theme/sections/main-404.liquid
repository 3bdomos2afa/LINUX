<section class="section wrap page-hero">
  <div class="empty-state glass" data-reveal>
    {% render 'mascot', name: 'classic', width: 180 %}
    <p class="mono muted">404</p>
    <h1>{{ 'page_404.title' | t }}</h1>
    <p class="lede" style="text-align:center">{{ 'page_404.body' | t }}</p>
    <a href="{{ routes.all_products_collection_url }}" class="btn btn--primary">{{ 'page_404.cta' | t }}{% render 'icon', name: 'arrow-right' %}</a>
  </div>
</section>
{% schema %}
{ "name": "404", "settings": [] }
{% endschema %}
